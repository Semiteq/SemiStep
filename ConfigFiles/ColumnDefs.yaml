# --- Example 
#- key: "initial_value"            # Уникальный ключ столбца. Используется в ActionsDefs.
#  business_logic:
#    property_type_id: "Float"     # Ссылка на ID из PropertyDefs.yaml. Указывает на тип данных в ячейке.
#    read_only: false              # false - столбец можно редактировать, true - нельзя.
#    plc_mapping:                  # Как значение будет отправлено в ПЛК (опционально). Если не указано то поле не будет передаваться в ПЛК.
#      area: "Float"               # Область памяти ПЛК ("Int", "Float").
#      index: 0                    # Смещение (индекс) внутри области для данной строки рецепта.
#  ui:
#    code: "initial_value"         # Внутренний код, обычно совпадает с key. Используется для записи и чтения csv.
#    ui_name: "Поток/Нач.знач."    # Заголовок столбца в таблице.
#    column_type: "property_field" # Тип UI-элемента (определяет фабрику для создания):
#                                  # action_combo_box - выпадающий список для Action, обязательно должен присутствовать.
#                                  # action_target_combo_box - выпадающий список для выбора цели действия, может быть несколько или не быть.
#                                  # property_field - стандарнтое поле ввода/вывода численных данных.
#                                  # step_start_time_field - особое поле времени начала шага, обязательно должно присутствовать.
#                                  # text_field - тексктовое поле.
#    max_dropdown_items: 10        # Максимальное количество видимых элементов в выпадающем списке (если column_type = action_combo_box или action_target_combo_box). Default = 30.
#    width: 160                    # Ширина столбца в пикселях. Должна быть >2. Можно задать -1, чтобы занять все оставшееся место (fill). Default = 130.
#    min_width: 60                 # Минимальная ширина столбца в пикселях. (должна быть >2). Default = 50.
#    alignment: 16                 # Выравнивание в ячейке (16=Left, 32=Center, 64=Right). Default = 16. (см https://learn.microsoft.com/en-us/dotnet/api/System.Windows.Forms.DataGridViewContentAlignment?view=netframework-4.8)
# (Детальное описание см. в документации)
# --- End Of Example ---

# Имя команды
- key: "action"
  ui:
    code: "action"
    ui_name: "Имя команды"
    column_type: "action_combo_box"
    width: 150
    alignment: 16
  business_logic:
    property_type_id: "Enum"
    read_only: false
    save_to_csv: true
    plc_mapping:
      area: "Int"
      index: 0

# Канал
- key: "channel"
  ui:
    code: "channel"
    ui_name: "Канал"
    column_type: "action_target_combo_box"
    width: 100
    alignment: 32
  business_logic:
    property_type_id: "Enum"
    read_only: false
    save_to_csv: true
    plc_mapping:
      area: "Int"
      index: 1

# Режим
- key: "mode"
  ui:
    code: "mode"
    ui_name: "Режим"
    column_type: "action_target_combo_box"
    width: 100
    alignment: 32
  business_logic:
    property_type_id: "Enum"
    read_only: false
    save_to_csv: true
    plc_mapping:
      area: "Int"
      index: 2

# Значение
- key: "target_value"
  ui:
    code: "target_value"
    ui_name: "Значение"
    column_type: "property_field"
    width: 160
    alignment: 32
  business_logic:
    property_type_id: "Float"
    read_only: false
    save_to_csv: true
    plc_mapping:
      area: "Float"
      index: 0

# Разбавление
- key: "dilution"
  ui:
    code: "dilution"
    ui_name: "Разбавление"
    column_type: "property_field"
    width: 230
    alignment: 32
  business_logic:
    property_type_id: "Float"
    read_only: false
    save_to_csv: true
    plc_mapping:
      area: "Float"
      index: 1

# Давление
- key: "pressure"
  ui:
    code: "pressure"
    ui_name: "Давление"
    column_type: "property_field"
    width: 110
    alignment: 32
  business_logic:
    property_type_id: "pressure_bar"
    read_only: false
    save_to_csv: true
    plc_mapping:
      area: "Float"
      index: 2

# Смешивание
- key: "mixing"
  ui:
    code: "mixing"
    ui_name: "Смешивание"
    column_type: "property_field"
    width: 140
    alignment: 32
  business_logic:
    property_type_id: "Flow"
    read_only: false
    save_to_csv: true
    plc_mapping:
      area: "Float"
      index: 3

# Транспорт
- key: "transport"
  ui:
    code: "transport"
    ui_name: "Транспорт"
    column_type: "property_field"
    width: 120
    alignment: 32
  business_logic:
    property_type_id: "Flow"
    read_only: false
    save_to_csv: true
    plc_mapping:
      area: "Float"
      index: 4

# Время
- key: "step_duration"
  ui:
    code: "step_duration"
    ui_name: "Время"
    column_type: "property_field"
    width: 120
    alignment: 32
  business_logic:
    property_type_id: "Time"
    read_only: false
    save_to_csv: true
    plc_mapping:
      area: "Float"
      index: 5

# Время абсолютное
- key: "step_start_time"
  ui:
    code: "step_start_time"
    ui_name: "Время абс."
    column_type: "step_start_time_field"
    width: 130
    alignment: 32
  business_logic:
    save_to_csv: false
    property_type_id: "String"
    read_only: true

# Комментарий
- key: "comment"
  ui:
    code: "comment"
    ui_name: "Комментарий"
    column_type: "text_field"
    width: -1
    alignment: 32
  business_logic:
    save_to_csv: true
    property_type_id: "String"
    read_only: false