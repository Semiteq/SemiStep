# 2. Требования к пользовательскому интерфейсу

[← Назад к оглавлению](index.md) | [← Общие сведения](01-overview.md)

---

## 2.1. Оконная модель

### 2.1.1. Окно редактирования/исполнения рецепта

Есть основное окно работы с рецептом, позволяющее:

- редактирование рецепта;
- запуск и мониторинг исполнения рецепта.

В этом окне отображаются:

- текущий активный рецепт;
- состояние исполнения (если есть связь с ПЛК/OPC);
- основные команды: загрузка/выгрузка в ПЛК, запуск/останов, переключение режимов отображения.

### 2.1.2. Окна исключительно для редактирования

Приложение должно поддерживать **произвольное количество** дополнительных окон, предназначенных:

- только для редактирования рецептов;
- без управления исполнением и загрузки в ПЛК.

В этих окнах:

- недоступны команды запуска/остановки исполнения;
- допустимы операции по редактированию, копированию, сохранению/загрузке рецептов с диска.

---

## 2.2. Общий вид окон

В каждом окне редактирования (и в основном окне исполнения):

- основная область с табличным представлением рецепта;
- панель инструментов с основными командами для данного типа окна;
- статус-бар с индикацией подключения к ПЛК/OPC и состояния синхронизации;
- панель сообщений/логов.

---

## 2.3. Табличное представление рецепта

### 2.3.1. Базовый режим (классический)

- Строки соответствуют шагам рецепта (1..N).
- Столбцы соответствуют параметрам и служебным полям шага.
- Заголовок столбцов отображает названия параметров.
- Поддерживаются до ~1000 шагов и до ~30 параметров.

### 2.3.2. Транспонированный режим

- Заголовки параметров отображаются **слева** (каждый параметр — отдельная строка).
- Сверху, слева направо, отображаются **номера шагов** (1..N).
- Данные рецепта в обоих режимах опираются на единую модель.
- Переключение между режимами осуществляется по установке параметра в настройках.

---

## 2.4. Редактирование и навигация

Редактирование ячеек по аналогии с табличными редакторами.

### Базовый набор горячих клавиш

| Комбинация | Действие |
|------------|----------|
| `Ctrl+C` | Копирование |
| `Ctrl+V` | Вставка |
| `Ctrl+X` | Вырезание |
| `Ctrl+N` | Добавление шага |
| `Del` | Удаление шага |
| `Ctrl+Z` | Отмена (Undo) |
| `Ctrl+Y` | Повтор (Redo) |

### Визуальная индикация

- выделение активной строки/ячейки;
- подсветка текущего шага исполнения (если есть данные от ПЛК/OPC).

---

## 2.5. Блокировка редактирования

При активном исполнении рецепта (по данным от ПЛК/OPC или внутреннему состоянию):

- редактирование может быть полностью или частично заблокировано;
- недоступны операции, которые могут привести к неконсистентности исполняемого рецепта:
  - удаление/добавление шагов;
  - изменение критичных параметров.

---

## 2.6. Отображение исполнения

### 2.6.1. Тонировка строк по глубине вложенности циклов

Строки таблицы рецепта тонируются цветом, отражающим глубину вложенности `For`-блока, к которому они относятся. Палитра задаётся в `ui/grid_style.yaml` в секции `colors.execution` и включает четыре уровня глубины (0..3) × два состояния (не пройдена / пройдена), что даёт восемь кистей фона. Установщик палитры (`ExecutionPaletteInstaller`) на старте приложения публикует их как ресурсы `ExecRowDepth{0..3}[Past]Brush`.

Правила тонировки:

- Строки `For` и `End_For` участвуют в тонировке наравне с шагами внутри блока — они получают цвет того блока, который ограничивают.
- Шаги, лежащие вне любого `For`-блока, отображаются базовым цветом строки (резерв `depth_0` оставлен для будущего явного стиля).
- На UI-уровне глубина ограничивается значением 3: при большей фактической вложенности применяется кисть `depth_3`. Само ядро (`RecipeSnapshot.RowLoopDepths`) ограничения не накладывает — отсечение выполняет `RecipeGridViewModel.RefreshRowLoopDepths`.

### 2.6.2. Маркер текущего шага в колонке номера

В колонке `№` (номер шага) текущий исполняемый шаг подсвечивается отдельной кистью `CurrentStepMarkerBrush` (источник — ключ `current_step_marker` из секции `colors.execution`). Маркер заливает фон ячейки номера и не зависит от выделения строки пользователем: при перемещении курсора подсветка остаётся на исполняемом шаге.

### 2.6.3. Статус-бар: оставшееся время

В статус-баре окна, помимо обычной индикации связи с ПЛК, отображаются:

- **Время до конца текущего шага** — длительность шага минус прошедшее с его начала время (по данным ПЛК). Шаги без явной длительности (`For`/`End_For`, пауза оператора, шаги ожидания по условию) в расчёт не входят.
- **Время до конца рецепта** — оставшееся время текущего шага плюс сумма длительностей последующих шагов. Те же шаги без длительности игнорируются. Развёртка `For`-блоков выполняется с учётом текущего значения счётчика итераций (отдаётся ПЛК).

Состояния отображения:

- Рецепт не загружен либо снапшот рецепта недоступен (анализ не выполнен или завершился ошибкой) — отображается прочерк (`—`) для обоих значений. Сам факт отсутствия данных от ПЛК прочерком не индицируется.
- Рецепт загружен, но не исполняется (включая отсутствие связи с ПЛК) — отображается `(00:00:00, TotalDuration)`: оператор видит полную длительность рецепта до нажатия `Start`.
- Рецепт исполняется — отображаются вычисленные значения по алгоритму выше.

Формат вывода:

- `чч:мм:сс` при длительности менее 24 часов;
- `d.чч:мм:сс` при длительности 24 часа и более (переключение автоматическое, по значению).

### 2.6.4. Выход из подрежима исполнения

После прекращения исполнения (ПЛК сбрасывает бит «рецепт исполняется» или окно переключается в `Edit` / теряет связь) времена в статус-баре переключаются на idle-отображение `(00:00:00, TotalDuration)` согласно §2.6.3.

### 2.6.5. Палитра отключённых (disabled / read-only) ячеек

Ячейки, доступные только для чтения (столбцы с `ReadOnly=true`), и ячейки, неприменимые для типа действия в строке (`InapplicableCellTheme.IsInapplicable=True`), отображаются палитрой, фон которой зависит от глубины вложенности цикла строки и от того, является ли шаг уже исполненным (past). Палитра задаётся в `ui/grid_style.yaml` в секции `colors.cells.disabled` десятью обязательными hex-ключами:
- восемь ключей фона по матрице «глубина × past» — `depth_0`, `depth_1`, `depth_2`, `depth_3` для строк без отметки past, и `depth_0_past`, `depth_1_past`, `depth_2_past`, `depth_3_past` для уже исполненных шагов. Структура повторяет матрицу `colors.execution` (см. §2.6.2);
- два общих ключа — `selected` (фон при выделении строки пользователем) и `foreground` (цвет текста).

Все цвета палитры наносятся с непрозрачностью 100 % — на экране отображается ровно тот hex, который указан в конфигурации, без смешения с фоном строки. Цвет `selected` побеждает цвет глубины: при выделении строки пользователем фон disabled-ячейки красится из `selected` независимо от глубины и past-состояния. Цвет `foreground` применяется универсально ко всем disabled-ячейкам вне зависимости от глубины строки. `GridStyleValidator` отклоняет конфигурацию, если секция или любой из десяти ключей отсутствует либо не является валидным hex-цветом. На старте приложения `CellPaletteInstaller` публикует эти значения и `colors.grid_line` как ресурсы `CellDisabledDepth0Brush`, `CellDisabledDepth1Brush`, `CellDisabledDepth2Brush`, `CellDisabledDepth3Brush`, `CellDisabledDepth0PastBrush`, `CellDisabledDepth1PastBrush`, `CellDisabledDepth2PastBrush`, `CellDisabledDepth3PastBrush`, `CellDisabledSelectedBackgroundBrush`, `CellDisabledForegroundBrush` и `GridLineBrush`, которые потребляют стили `DataGridStyles.axaml`.

---

[Далее: Модель данных →](03-data-model.md)
